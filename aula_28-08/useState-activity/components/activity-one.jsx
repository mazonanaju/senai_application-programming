import { useState } from 'react'
import './components/activity-one.css'

function activityOne{
    const[text, setText] = useState()
    <button onClick={() => setText((text => text.style.display ='none'))}>batata</button>
  
}
